# 答辩速查卡

## 项目一句话

> 校园导航系统，CLI 批处理，读命令 → 调 C++ 图算法 → 输出结果。5 个模块分层调用，嵌套 unordered_map 存无向图，所有算法只走 status=open 的边。

---

## 架构速查

```
main.cpp 读 cin → CommandProcessor 解析命令 → 分发到 CsvIO / LGraph / Algorithm
```

| 模块 | 文件 | 职责 |
|------|------|------|
| LGraph | `LGraph.h/cpp` | 图数据结构，点/边增删改查 |
| CsvIO | `CsvIO.h/cpp` | CSV 读写 |
| CommandProcessor | `CommandProcessor.h/cpp` | 解析命令，格式化输出 |
| Algorithm | `Algorithm.h/cpp` | 所有图算法 |

---

## 数据结构速查

```cpp
unordered_map<string, LocationInfo> vertices_;   // place_id → 地点
unordered_map<string, unordered_map<string, EdgeNode>> adj_;  // from → {to → 边}
int edge_count_;
bool directed_;
```

**为什么选嵌套 unordered_map？** 增删查 O(1)，删顶点只遍历邻接表 O(deg(v))，无需全图扫描。

---

## 七算法速查表

### 1. 连通分量 — BFS

```
在哪：Algorithm.cpp → GetConnectedComponents / bfsComponent
复杂度：O(V+E)
核心：遍历所有点，未访问就起 BFS（只走 open 边），一圈一圈扩
特殊：bfsComponent 支持 blocked_vertex / blocked_edge 参数，给 Critical 复用
输出：COMPONENTS <count> SIZES <s1> <s2> ...（降序）
```

**答辩一句话**："BFS 涟漪扩散。visited 集合去重，queue 存待扩散点。碰到 blocked 就当没看见。"

---

### 2. Dijkstra 最短路径

```
在哪：Algorithm.cpp → GetShortestPath
复杂度：O((V+E) log V)
核心：小顶堆 priority_queue，每次取 dist 最小的未确定点，松弛邻居
模式：DIST → e.distance，TIME → e.walk_time，其余代码完全一样
平局：nd == dist[v] && u < prev[v] → 选字典序更小的前驱
输出：PATH <DIST|TIME> <cost> NODES <id1> <id2> ...
不可达：NO_PATH
```

**答辩一句话**："站在当前最近的点，看能不能帮邻居变得更近。优先队列保证每次取到的一定是确定的。dist 和 prev 用 unordered_map，因为 place_id 是字符串。"

**TA 追：pq 里可能有旧数据怎么处理？** "`if (d != dist[u]) continue;` 跳过。"

**TA 追：为什么用 unordered_map 不用 vector？** "place_id 是 P0001 这种字符串，不是连续整数，unordered_map 直接以字符串为键。"

---

### 3. 时刻约束最短路径

```
在哪：Algorithm.cpp → GetTimedShortestPath
复杂度：O((V+E) log V)，和普通 Dijkstra 一样
核心：在 Dijkstra 基础上多检查一行
```

```cpp
// 松弛邻居 v 时多查这行：
auto info = graph.GetVertex(v);
if (time < info.open_time || time > info.close_time) continue;
// 起止点也提前检查——不开放直接返回不可达
```

**答辩一句话**："普通 Dijkstra + 一个过滤器。松弛前问'你现在开门吗'，不开就跳过。字符串比 HH:MM 天然正确。"

---

### 4. 必经点路径

```
在哪：Algorithm.cpp → GetMustPassPath
复杂度：O(k·(V+E) log V)，k=必经点数
核心：拆成 from→w1, w1→w2, ..., wk→to，逐段调 GetShortestPath
拼接：第一段全要，后面每段跳第一个点（上一段的终点）
任一段不可达 → 整体 NO_PATH
```

**答辩一句话**："把一条路拆成 k+1 段，每段跑 Dijkstra，首尾相接。中间有断的整条路就废了。"

**TA 追：为什么拼接要跳第一个点？** "第一段的终点就是第二段的起点，同一个点不能出现两次。"

---

### 5. Kruskal 最小生成树

```
在哪：Algorithm.cpp → MinimumSpanningTree
复杂度：O(E log E)，排序占大头
核心三步：
  1. 所有 open 边按 distance 排序（等权按 canonical key 字典序平局）
  2. DSU 贪心：不连通就加，连通就跳
  3. 边数 = V-1 ✓  边数 < V-1 → 图不连通 → DISCONNECTED
```

**答辩一句话**："所有边从小到大排队，DSU 判两头通不通。不通就修路连上，通了就跳过（修了反而成环）。"

**TA 追：等权边导致 MST 不唯一怎么办？** "排序加了 canonical key 副键，保证确定性。10000 节点等权极多时数学上确实存在多个等价 MST，12/13 通过，1 个残余总权相同。"

**TA 追：为什么 Kruskal 不选 Prim？** "DSU 已写好了直接复用，稀疏图 Kruskal O(E log E) 够快，而且我不会因为在所有节点上找最小边而浪费太多时间。"

---

### 6. 关键节点/边（暴力）

```
在哪：Algorithm.cpp → FindCriticalNodesAndEdges
复杂度：O(V·(V+E)) + O(E·(V+E))
核心：
  1. 算基线 baseline 分量数
  2. 逐个屏蔽点 → 重算分量数 → > baseline 就是关键节点
  3. 逐个屏蔽边 → 重算分量数 → > baseline 就是关键边
屏蔽通过 blocked 参数传给 bfsComponent，不实际改图
```

**答辩一句话**："基线跑一次 BFS 知道有几个分量，然后逐个实验'假如没有这个点/边'——分量数变多了就是关键。"

**TA 追：为什么不直接删点？** "真删要改图再还原，太麻烦。blocked 参数就是'假装没看见'，干净。"

**TA 追：大图会不会太慢？** "1000 节点 chain_1000 能过。10000 节点如果全测完会很慢，还好评测用例是这个规模的稀疏图，Tarjan 可以 O(V+E) 解决。"

---

### 7. 分层图 SHORTEST_K（拓展1）

```
在哪：Algorithm.cpp → GetShortestPathK
复杂度：O(K·(V+E)·log(V·K))，K≤10
核心：状态从 (地点) 扩展为 (地点, 已用券数)，K+1 层图
转移：
  不用券：dist[v][k] = min(自己, dist[u][k] + walk_time)    ← 同层
  用券：  dist[v][k+1] = min(自己, dist[u][k] + ceil(w/3))  ← 向上一层
```

**关键反例**：A-B-C-D (10,10,10) vs A-X-D (5,50)，K=1。贪心 24，分层图 22。

**回溯**：prev_node[v][k] 记录前驱地点，prev_k[v][k] 记录前驱 k 值，coupon[v][k] 标记是否用券。从 (to, best_k) 退到 (from, 0)。

**答辩一句话**："不是在一张图上找路，是在 K+1 层图上找路。每层代表'用了多少券'，用券就升一层、时间打三折。Dijkstra 保证第一次弹出终点就是最优。"

---

## TA 高频追问速答

| 问题 | 秒答 |
|------|------|
| 无向图怎么存？ | 双向各写一份，删点遍历邻接表清反向 |
| 为什么不用邻接矩阵？ | 10000 节点矩阵 400MB，哈希表稀疏才几十 MB |
| unordered_map 最坏 O(n) 怎么办？ | std::string 哈希够均衡，评测规模下没碰到退化 |
| DIST 和 TIME 怎么做到一份代码？ | mode 参数，选 distance 还是 walk_time，一行区别 |
| MST 那条为啥不过？ | 等权边 MST 不唯一，双方都是对的，46/9999 差异 |
| 你怎么测的？ | diff --strip-trailing-cr 逐行对比，17 个用例 16 个全对 |
| AI 帮了多少？ | 必做核心算法连通分量/Dijkstra/必经点/MST/Critical 是我自己写的；拓展功能（分层图/Tarjan/UI）和报告初稿 AI 辅助 |

---

## 代码位置速查

| TA 说"打开看看" | 打开哪个文件 | 大概位置 |
|------|------|------|
| 图怎么存的 | LGraph.h | private 区的 adj_ 定义 |
| BFS 连通分量 | Algorithm.cpp | `bfsComponent` 函数 |
| Dijkstra | Algorithm.cpp | `GetShortestPath` |
| MST | Algorithm.cpp | `MinimumSpanningTree` |
| Critical 暴力 | Algorithm.cpp | `FindCriticalNodesAndEdges` |
| Tarjan | Algorithm.cpp | `FindCriticalTarjan` |
| 分层图 | Algorithm.cpp | `GetShortestPathK` |
| 命令怎么分发的 | CommandProcessor.cpp | `ProcessCommand` |
| 命令输出格式 | CommandProcessor.cpp | 对应的 `cmd*` 函数 |
