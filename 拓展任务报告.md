# 拓展任务报告

## 拓展 1：分层图最短路径 SHORTEST_K（9 分）

### 1.1 场景

校园内提供共享单车/接驳车券：每张券可将一条边的步行耗时从 `walk_time` 缩短为 `ceil(walk_time / 3)`（骑行速度约为步行 3 倍）。每位学生有最多 K 张券（0 ≤ K ≤ 10），每券绑定一条边，不必用满。

### 1.2 算法设计：K 层状态图 Dijkstra

#### 核心思想

将"已用券数"作为额外状态维度，在逻辑上将原图展开为 K+1 层。第 k 层表示"恰好用了 k 张券到达该节点的最短时间"。

**状态定义**：`dist[place_id][k]` = 到达地点 `place_id`、恰好使用 k 张券的最短时间。

**状态转移**（对每条 status=open 的边 u→v，walk_time = w）：

```
分支 1（不用券）：dist[v][k] = min(dist[v][k], dist[u][k] + w)          — 同层转移
分支 2（用券）：  dist[v][k+1] = min(dist[v][k+1], dist[u][k] + ceil(w/3))  — 向上一层转移
```

#### 为什么 Dijkstra 仍然正确

扩展后的状态图中所有边权仍非负（w ≥ 0，ceil(w/3) ≥ 0），Dijkstra 的贪心性质不变——优先队列中第一次弹出某个 `(place_id, k)` 状态时即为其最短距离。

#### 关键反例：为什么不能贪心

```
A ——10min—— B ——10min—— C ——10min—— D
A —— 5min—— X ——50min—— D
```

K=1 时的最短到达时间：

| 策略 | 路径 | 计算 | 结果 |
|------|------|------|:--:|
| **贪心**（先 Dijkstra 再在最长边上用券） | A→B→C→D，在 AB 上用券 | ceil(10/3)+10+10 = 4+20 | **24** |
| **分层图**（并行搜索所有券分配方案） | A→X→D，在 XD 上用券 | 5+ceil(50/3) = 5+17 | **22** ✓ |

贪心失败原因：路径选择与用券决策相互耦合。先确定路径再选边用券会错过"走远路但用券后更优"的方案。分层图在每个状态中同时优化两者。

### 1.3 实现细节

**数据结构**：

```cpp
struct PathResultK {
    int total_time;                              // 最短总时间
    int k_used;                                  // 实际用券数
    std::vector<std::string> path;               // 节点序列
    std::vector<std::pair<std::string,std::string>> fast_edges; // 用券边
    bool reachable;
};

// 稀疏二维状态存储
unordered_map<string, unordered_map<int, int>> dist;
unordered_map<string, unordered_map<int, string>> prev_node;
unordered_map<string, unordered_map<int, int>> prev_k_map;
unordered_map<string, unordered_map<int, bool>> coupon;
```

**算法流程**：

1. 初始化 `dist[from][0] = 0`，push `(0, from, 0)` 入优先队列
2. 弹堆顶 `(time, u, k)`
   - 陈旧检测：`time != dist[u][k]` → 跳过
   - 终点检测：`u == to_id` → break（第一次弹出即最优）
   - 遍历 u 的邻接边（仅 open）
     - **不用券**：`nd = time + w`，若 < dist[v][k] 则更新
     - **用券**（k+1 ≤ K）：`nd = time + (w+2)/3`，若 < dist[v][k+1] 则更新
3. 从 `(to_id, best_k)` 回溯到 `(from, 0)`，记录用券边
4. 用券边按 `(min, max)` 字典序排序后输出

**复杂度**：时间 O(K·(V+E)·log(V·K))，空间 O(V·K)。K ≤ 10 时 ≈ O((V+E)·log V)。1000 节点测试可秒级完成。

### 1.4 涉及文件

| 文件 | 修改内容 |
|------|----------|
| `Algorithm.h` | 新增 `PathResultK` 结构体、`GetShortestPathK` 声明 |
| `Algorithm.cpp` | 新增 `<limits>` `<tuple>` 头文件，实现完整分层图 Dijkstra（~110 行） |
| `CommandProcessor.h` | 新增 `cmdShortestK` 声明 |
| `CommandProcessor.cpp` | 新增 `SHORTEST_K` 命令分发与格式化输出 |
| `graph_ui.html` | 前端实现 `dijkstraK`，新增 K 值输入框和 SHORTEST_K 模式选项 |

### 1.5 测试结果

| 用例 | 规模 | 结果 | 验证要点 |
|------|------|:--:|------|
| small/case_layered_01 | 5 节点 | ✅ | K=0,1,2,3 全覆盖，验证反例 A-B-C-D vs A-X-D |
| medium/random_sparse_100 | 100 节点 | ✅ | 随机稀疏图，K=3 |
| medium/grid_100 | 100 节点 | ✅ | 网格图多等权路径，K=5 |
| large/random_sparse_1000 | 1000 节点 | ✅ | 大规模压力，K=5 |

**通过率：4/4（100%）**

### 1.6 评分对照

| 评分维度 | 分值 | 自评 |
|----------|:--:|------|
| 算法正确性（K=0 / 中等 K / 边界 K=10） | 5 | ✅ 四种 K 值覆盖，与标准答案 diff 完全一致 |
| 路径回溯 + 输出用券位置 | 2 | ✅ PATH/K_USED/NODES/FAST 格式完整，FAST 边字典序排序 |
| 报告中说明实现思路与复杂度 | 2 | ✅ |

---

## 拓展 2：自定义微型数据集 + 对抗样例（3 分）

### 2.1 微型数据集：华师大普陀校区周边交通网络

#### 数据来源

基于华东师范大学普陀校区（上海市普陀区中山北路 3663 号）周边真实地理环境构建。数据来源：百度地图距离测算、上海地铁官网站间距、Moovit 公交查询。

#### 数据集规模

- 12 个节点（地铁站 ×6、购物中心 ×3、校园地标 ×1、公园 ×1、科技园 ×1）
- 40 条边（38 条步行边 + 2 条地铁边）

#### 建模特点

1. **双层交通网络**：步行层（38 条边，距离适中但时间接近实际步行速度 ~80m/min）+ 地铁层（2 条边，P0002↔P0005 和 P0002↔P0009，距离与步行相近但时间仅 2min）。DIST 和 TIME 模式会选出不同路径。
2. **时间窗口多样性**：商场 10:00-22:00、公园 06:00-20:00、地铁 05:30-23:30、公交站和校门全天开放——覆盖了 TIMED_SHORTEST 的各种时间约束组合。
3. **拓扑特征**：苏州河将区域天然分为南北两岸，P0002（金沙江路站）是唯一的地铁过江枢纽，形成图的关键节点。

#### 可检验的功能

| 命令 | 检验点 |
|------|--------|
| LOAD | 12 节点 40 边正确加载 |
| SHORTEST DIST vs TIME | 步行边距离短而慢 vs 地铁边距离长而快，两模式给不同结果 |
| TIMED_SHORTEST | 清晨商场未开门时绕行，晚间公园关闭后绕行 |
| COMPONENTS | 12 节点全连通 |
| MST | 应选出 11 条步行短边为主的生成树 |
| CRITICAL | P0002 为关键节点，跨河地铁边为关键边 |
| SHORTEST_K | 步行+地铁双层网络上用券加速，可能产生新最优路径 |

#### 交付文件

```
自定义数据集/华师大普陀校区/
├── places.csv    # 12 节点，中文名称，6 种分类，差异化时间窗口
├── roads.csv     # 40 条边（38 步行 + 2 地铁），区分 DIST/TIME
└── 说明文档.md   # 数据来源、建模特点、检验功能清单
```

### 2.2 对抗样例

#### 对抗目标：TIMED_SHORTEST 中间节点时间窗口检查

**被卡住的错误实现**：只检查起点和终点在查询时刻是否开放，但不在 Dijkstra 松弛循环中对每个途经节点做时间检查。

#### 对抗图构造

8 个节点、20 条边的校园微型图。核心对抗路径：

```
P0001(宿舍,全天) ——2min—— P0003(食堂,06:00-21:00) ——2min—— P0007(南门,全天)
P0001(宿舍,全天) ——8min—— P0006(体育场,06:00-22:00) ——4min—— P0007(南门,全天)
```

**测试命令**：`TIMED_SHORTEST P0001 P0007 22:00 TIME`

| 时刻 | 错误实现 | 正确输出 | 说明 |
|------|:--:|:--:|------|
| 12:00（食堂开放） | 4min | 4min | 一致，都走短路径 |
| **22:00（食堂关闭）** | **4min** ✗ | **12min** ✓ | 错误实现未检查 P0003，穿过了已关闭节点 |

#### 我的程序如何正确处理

`GetTimedShortestPath` 在 Dijkstra 松弛每个邻居 v 时：

```cpp
auto info_v = graph.GetVertex(v);
if (time < info_v.open_time || time > info_v.close_time) continue;
```

路径上每一个中间节点都在查询时刻被校验。食堂 P0003 在 22:00 关闭，松弛时被跳过，Dijkstra 自然选绕路方案。

#### 交付文件

```
自定义数据集/对抗样例/
├── places.csv    # 8 节点，含关键时间约束
├── roads.csv     # 20 条边，构造短/绕对比路径
└── 说明文档.md   # 对抗目标、卡住的错误、正确做法、实测结果
```

### 2.3 评分对照

| 评分维度 | 分值 | 自评 |
|----------|:--:|------|
| 微型数据集 + 说明 | 1.5 | ✅ 12 节点 40 边，真实地理数据，双层网络建模，附完整说明文档 |
| 对抗样例 + 说明 | 1.5 | ✅ 针对 TIMED_SHORTEST 中间节点检查，附说明文档 + 实测验证 |

---

## 拓展 3：图形化界面（3 分）

### 3.1 技术方案

**HTML5 Canvas + 原生 JavaScript**，单文件自包含（`graph_ui.html`，约 1200 行），零外部依赖。浏览器直接打开即可运行，无需编译、无需服务器。

### 3.2 布局引擎

**力导向 + 同心圆环**双层策略：

- **连通分量检测**：仅遍历 open 边分割图，分量按大小降序
- **主连通分量**：Fruchterman-Reingold 力导向算法，300 轮迭代 + 温度冷却，置于画布中央
- **非连通分量**：从主分量包围半径 +350px 处开始，排布在外围同心圆上，每层 10 个分量，分量内节点沿圆弧均匀分布

### 3.3 必须展示项

| 要求 | 实现 |
|------|------|
| **节点位置** | ✅ 力导向自动布局 + 同心圆环补位 |
| **边连接，区分 open/closed** | ✅ 绿色实线(open) / 红色虚线(closed) / 蓝色虚线(地铁) |
| **最短路径高亮** | ✅ 红色粗线+发光阴影+每段权重标注，支持 DIST / TIME / SHORTEST_K 三种模式 |
| **关键节点/边标识** | ✅ 关键节点黄色虚线环 + 关键边橙色加粗线，右侧面板显示列表 |

### 3.4 自主发挥亮点

| 功能 | 说明 |
|------|------|
| **SHORTEST_K 分层图交互** | 前端实现完整分层图 Dijkstra，K 值输入框，用券边 ⚡ 标注加速后时间 |
| **连通分量可视化** | 凸包半透明着色 + 同心圆环虚线引导圈 |
| **视图交互** | 滚轮缩放(0.12x~6x)、拖拽平移、双击放大 |
| **HiDPI 自适应** | devicePixelRatio 正确计入渲染变换 |
| **通用数据加载** | 内置项目 87 节点 26 边，支持文件选择器加载任意 CSV |

### 3.5 前端算法实现

| 算法 | 实现 | 复杂度 |
|------|------|------|
| Dijkstra（DIST/TIME） | 线性扫描 | O(V²) |
| 分层图 Dijkstra（SHORTEST_K） | 二维状态 + 线性扫描 | O(K·V²) |
| 连通分量 | BFS + blocked 集合 | O(V+E) |
| 关键节点/边 | 暴力连通分量重算 | O(V·(V+E) + E·(V+E)) |

前端算法与 C++ 后端逻辑一致，数据均来自同一 CSV 解析，可通过 diff 交叉验证。

### 3.6 运行方式

```bash
# Windows 双击
start CampusNavigation/graph_ui.html

# macOS
open CampusNavigation/graph_ui.html
```

默认展示项目完整数据（87 节点 / 26 边）。CLI 入口完全保留不受影响。

### 3.7 提交物

| 提交物 | 状态 |
|--------|:--:|
| `graph_ui.html`|见文件夹内截图|
| 演示视频（可选） | 见文件夹内视频 |
| 技术说明文档 | ✅ `图形化界面技术说明.md` |

### 3.8 评分对照

| 评分维度 | 分值 | 自评 |
|----------|:--:|------|
| 必须展示项全部达标 | 2 | ✅ 四项全部覆盖 |
| 在必须展示之外有亮点 | 1 | ✅ SHORTEST_K 交互、连通分量可视化、HiDPI 自适应、深色主题、力导向+同心圆混合布局 |
