# 校园导航系统 — 实验报告

## 1. 数据结构设计说明

### 1.1 选型：嵌套 unordered_map

```cpp
std::unordered_map<std::string, LocationInfo> vertices_;  // place_id → 顶点
std::unordered_map<std::string,
    std::unordered_map<std::string, EdgeNode>> adj_;       // from → {to → 边}
bool directed_;
int edge_count_;
```

### 1.2 为什么选这种结构

- **顶点/边增删查均为平均 O(1)**。`unordered_map` 以 place_id（字符串）为键直接哈希，无需维护 ID→下标映射，代码简洁。
- **无向图双向存储**：插入边时同时写入 `adj_[u][v]` 和 `adj_[v][u]`。删除顶点时只需遍历其邻接表（大小 = deg(v)），反向边可直接定位删除，无需全图扫描。
- **对 10000 节点规模充裕**。课程评测的最大数据为 10000 节点/稀疏边，嵌套 `unordered_map` 的空间开销和哈希性能均满足要求。
- **与动态更新场景匹配**：频繁增删点边时，邻接矩阵需要 O(V²) 重分配，邻接表+索引映射需要删点后重排索引；嵌套 map 的删除是局部操作，影响范围仅限被删顶点的邻居。

### 1.3 考虑过的替代方案

| 方案 | 思路 | 权衡 |
|------|------|------|
| **邻接表 + 索引映射** | `vector<Vertex>` 存顶点，`vector<list<Edge>>` 存邻接表，`unordered_map<string,int>` 做 ID→下标映射 | 删顶点时需全局重排索引或标记删除，实现复杂；遍历时缓存友好 |
| **邻接矩阵** | `vector<vector<int>>` 存边权 | 顶点增删 O(V²)，10000 节点时矩阵约 400MB，不可接受 |
| **单层 map + 边集** | 点用 `unordered_map` 存，边用 `unordered_set<string>` 存 key（"u&#124;v"） | 删顶点时需遍历整个边集匹配 key，O(E)，而嵌套 map 仅 O(deg(v)) |

**最终选择嵌套 unordered_map**：删顶点 O(deg(v))，无需全局扫描，在动态更新场景下综合最优。

### 1.4 操作复杂度

#### 顶点

| 操作 | 时间 | 说明 |
|------|------|------|
| `InsertVertex` | O(1) 平均 | 哈希插入 |
| `DeleteVertex` | O(deg(v)) 平均 | 遍历邻接表清理反向边 |
| `UpdateVertex` | O(1) 平均 | 查找后修改字段 |
| `GetVertex` | O(1) 平均 | 查找返回副本 |
| `Clear` | O(V) | 清空 vertices_、adj_，重置 edge_count_ |

#### 边

| 操作 | 时间 | 说明 |
|------|------|------|
| `InsertEdge` | O(1) 平均 | 无向图双向各插入一次 |
| `DeleteEdge` | O(1) 平均 | 双向删除 |
| `UpdateEdge` | O(1) 平均 | 双向同步字段 |

#### 遍历

| 操作 | 时间 | 说明 |
|------|------|------|
| `AllPlaceIds` | O(V) | 遍历 vertices_ 收集 key |
| `AllEdges` | O(V+E) | 双层遍历 adj_，无向图 `from < to` 去重 |
| `GetAdjacentEdges` | O(deg(v)) | 遍历邻接表 |
| `GetPlacesByCategory` | O(V) | 全表扫描 |

---

## 2. 关键算法说明与复杂度分析

### 2.1 DSU（并查集）

**实现**：`parent` 数组存父节点，`rank_` 数组存树高上界。

- **find(x)**：递归找根，路径压缩 `parent[x] = find(parent[x])`，平摊 O(α(n))
- **unite(x, y)**：按秩合并，始终将矮树挂到高树下，O(α(n))

用于 Kruskal MST 和连通性辅助判断。

### 2.2 连通分量分析（BFS）

**实现**：遍历所有顶点，对每个未访问顶点启动 BFS。BFS 仅沿 `status=open` 的邻边扩散。每轮 BFS 访问到的顶点数即为该分量规模。最后按规模降序排列。

内部辅助函数 `bfsComponent` 支持 `blocked_vertex` 和 `blocked_edge_keys` 参数，供关键节点/边分析复用——临时屏蔽某节点或某边后重算连通分量。

**复杂度**：O(V + E)，每个顶点和每条 open 边恰好访问一次。

### 2.3 Dijkstra 最短路径（双模式 + 时刻约束 + 必经点）

**普通 Dijkstra**：优先队列（小顶堆）优化，`unordered_map<string,int>` 存 dist，`unordered_map<string,string>` 存 prev。

- **DIST 模式**：边权取 `distance`
- **TIME 模式**：边权取 `walk_time`
- 仅遍历 `status=open` 的边
- 等权时选字典序更小的前驱（`u < prev[v]`），确保路径输出确定性

**时刻约束 Dijkstra**：在松弛邻居 v 时额外检查 `open_time ≤ time ≤ close_time`（字符串比较）。起点或终点不开放直接返回不可达。

**必经点路径**：将路径拆为 `from→w1→w2→...→to`，逐段调用普通 Dijkstra。拼接时跳过 junction 点的重复。任一段不可达则整体不可达。

**复杂度**：O((V+E) log V)。必经点为 O(k·(V+E) log V)，k = 必经点数。

### 2.4 最小生成树（Kruskal + DSU）

**实现**：
1. 从排序后的 `AllPlaceIds` 确定性遍历收集所有 open 边（避免 `unordered_map` 迭代顺序不确定）
2. 按 `(distance, canonical_key)` 排序，等权时按 `min(from,to)|max(from,to)` 字典序平局
3. DSU 维护连通分量，贪心加边
4. 若最终边数 ≠ V-1，返回空（DISCONNECTED）
5. 输出边按 `(min, max)` 字典序排序

**选择 Kruskal 的理由**：DSU 已在项目中实现，可直接复用。Kruskal 对稀疏图效率高（本项目的图均为稀疏图）。Prim 在等权边多的图上（如 10000 节点随机稀疏图）会产生与 Kruskal 等价但不相同的 MST。

**复杂度**：O(E log E)，瓶颈在边排序。

### 2.5 关键节点与关键边（暴力连通分量重算）

**实现**：
1. 计算 baseline 连通分量数（仅 open 边，无屏蔽）
2. 关键节点：依次屏蔽每个顶点（设为 blocked_vertex），重算分量数。若 > baseline，则该顶点为关键节点
3. 关键边：依次屏蔽每条 open 边，重算分量数。若 > baseline，则该边为关键边
4. 复用 `bfsComponent` 内部函数，屏蔽通过参数传递，**不实际修改图**

**复杂度**：O(V·(V+E)) + O(E·(V+E))。对 1000 节点规模可接受（chain_1000 测试通过）。

#### 加分项：Tarjan 算法 O(V+E)

实现了 `FindCriticalTarjan` 作为暴力方法的优化对照：

**原理**：一次 DFS 遍历，维护 `dfn[u]`（首次访问时间戳）和 `low[u]`（通过回边能到达的最小 dfn）。对非根节点 u，若存在子节点 v 使得 `low[v] ≥ dfn[u]`，则 u 是割点；若 `low[v] > dfn[u]`，则边 (u,v) 是桥。根节点若在 DFS 树中有 >1 个孩子则为割点。

**实现**：非递归 DFS（显式栈）避免深层递归爆栈。place_id → 整数索引映射，`vector<int>` 存 dfn/low，`vector<bool>` 存 is_cut，`vector<pair<int,int>>` 存 bridges。

**复杂度**：时间 O(V+E)，空间 O(V)。与暴力方法在全部 13 个测试用例上输出完全一致。

### 2.6 分层图最短路径 SHORTEST_K（拓展1）

**状态扩展**：`dist[place_id][k]` = 到达某地且恰好用 k 张券的最短时间。

**转移**（对每条 open 边 u→v，walk_time = w）：
1. 不用券：`dist[v][k] = min(dist[v][k], dist[u][k] + w)`
2. 用券（k+1 ≤ K）：`dist[v][k+1] = min(dist[v][k+1], dist[u][k] + ceil(w/3))`

**实现**：优先队列存 `tuple<time, place_id, k>`。第一次弹出终点即全局最优。`prev_node[v][k]` + `prev_k[v][k]` 记录前驱状态，`coupon[v][k]` 标记用券边。

**关键反例（卡贪心）**：A-B-C-D (walk=10,10,10)，A-X-D (5,50)，K=1。贪心先求最短路再选最长边用券得 24；分层图枚举并行路径得 22（走 A-X-D，在 X-D 用券）。

**复杂度**：O(K·(V+E)·log(V·K))，K ≤ 10 时 ≈ O((V+E)·log V)。

---

## 3. 测试方案与结果

### 3.1 测试环境与流程

**环境**：MinGW g++ 8.1.0（C++17）、CMake + Ninja、Git Bash（Windows）。

**我制定的测试流程**：

每完成一个阶段，我要求 DeepSeek 立即构建并跑测试。每一步验证阶段成果：

1. **编译**：`cmake -S CampusNavigation -B build -G "Ninja" && cmake --build build --config Debug`
2. **进入测试用例目录**：因为 `command.txt` 里 `LOAD` 用的是相对路径 `places.csv`，必须 cd 到数据所在目录
3. **批处理运行**：`../../build/CampusNavigation < command.txt > my_output_test.txt`
4. **逐行对比**：`diff --strip-trailing-cr my_output_test.txt answer.txt`
5. **如果失败**：查看 diff 输出 → 定位差异行 → 分析是哪个命令/算法 → 回到代码修 → 重复 1-4

这个流程贯穿始终，从阶段 3 的第一个 case_01 到最后全部 17 个用例。每条 commit 记录背后都对应一次完整的构建-运行-diff 循环。

### 3.2 分阶段测试过程

**阶段 3 完成时（数据维护 + 查询命令）**：仅测试 case_01。前 20 行（LOAD 到 DELETE_PLACE 之间的所有数据操作和查询）与答案完全一致。第 21-26 行算法命令仍输出 `ERROR not_implemented`，因为阶段 4 还没做——这是预期行为，说明命令分发框架正确。

**阶段 4-5 完成时（所有算法实现 + 接入）**：首次跑全部 13 个必做用例。结果：

- sample_ecnu、small 4 个：全部通过
- medium 6 个：chain_flower_100、mixed_time_100、star_100 通过；**grid_100、ring_100、random_sparse_100 失败**
- large 2 个：chain_1000 通过；**random_sparse_10000 失败**

四个失败的用例，diff 全部集中在 MST 行——总距离相同，但边集合选的不一样。
 
**MST 排错过程**：我让 DeepSeek 按步骤排查。先确认 Kruskal 逻辑本身没错（距离排序 → DSU 贪心 → 边数验证），再检查等权边的处理顺序。根因找到了：`AllEdges` 遍历 `unordered_map`，迭代顺序不确定，导致每次收集到的边列表顺序不一样。修复分两步：(1) 改为从排序后的 `AllPlaceIds` 确定性遍历收集边，不再依赖 unordered_map 迭代；(2) 排序增加 canonical key（`min(from,to)|max(from,to)`）副键平局。

修复后重跑：grid_100、ring_100、random_sparse_100 三个通过。但 random_sparse_10000 仍失败——MST 总权相同，9953/9999 条边一致，仅 46 条不同。

**grid_100 的另一个 bug**：MST 修好后，SHORTEST TIME 又暴露了新问题——路径节点序列与答案不同，但总时间都是 18。原因是网格图中多条等时路径，Dijkstra 的 `nd < dist[v]` 保留了先找到的那条，而"先找到哪条"依然取决于 unordered_map 的邻接遍历顺序。修复：等权时选字典序更小的前驱（`nd == dist[v] && u < prev[v]`）。

**分层图测试**：拓展 1 的 4 个用例首次全跑就全部通过。但 large 用例 FAST 边顺序不对——路径回溯给的是遍历顺序，评测要求字典序。在输出前加了 `std::sort` 修复。

### 3.3 最终测试结果

**必做部分（13 个用例）**：

| 类别 | 用例 | 规模 | 结果 | 备注 |
|------|------|------|:--:|------|
| sample | sample_ecnu | 87节点/26边 | ✅ | 项目自带完整数据 |
| small | case_01 | 6/8 | ✅ | 基础功能全覆盖 |
| small | case_02 | — | ✅ | |
| small | case_03 | — | ✅ | |
| small | case_04 | — | ✅ | |
| medium | chain_flower_100 | 100节点 | ✅ | 链+花混合拓扑 |
| medium | grid_100 | 100节点 | ✅ | 等权路径平局 |
| medium | mixed_time_100 | 100节点 | ✅ | 时刻约束全覆盖 |
| medium | random_sparse_100 | 100节点 | ✅ | 随机稀疏图 |
| medium | ring_100 | 100节点 | ✅ | 环形拓扑 |
| medium | star_100 | 100节点 | ✅ | 星形拓扑 |
| large | chain_1000 | 1000节点 | ✅ | 998个关键节点 |
| large | random_sparse_10000 | 10000节点 | ❌ | 见下方说明 |

**通过率：12/13（92.3%）**

**拓展1 分层图（4 个用例）**：

| 类别 | 用例 | 规模 | 结果 |
|------|------|------|:--:|
| small | case_layered_01 | 5节点 | ✅ |
| medium | random_sparse_100 | 100节点 | ✅ |
| medium | grid_100 | 100节点 | ✅ |
| large | random_sparse_1000 | 1000节点 | ✅ |

**通过率：4/4（100%）**

**总计：17 个用例，16 个通过。**

### 3.4 random_sparse_10000 差异说明

唯一未通过的用例。差异仅出现在第 5 行（MST 输出）：双方总距离均为 **41994**，边数均为 **9999**，其中 9953 条边完全一致，仅 46 条边不同（各选了 46 条不同的等价边）。

**根因**：10000 节点随机稀疏图中有极大量的等距离边。Kruskal 在处理等权边时的选择与参考实现不同，导致产生不同的 MST 边集合——但两者都是数学上正确的最小生成树（总权相同，且均为图中可能的最小值）。

**我尝试过的修复及结果**：

| 尝试 | 方法 | 结果 |
|------|------|:--:|
| 1 | canonical key 字典序副键 | medium 三个通过，random_sparse_10000 仍不过 |
| 2 | raw from_id/to_id 排序 | 同上 |
| 3 | string concat 排序 | 同上 |
| 4 | 替换为 Prim 算法 | 总权相同，边集合依然不同 |
| 5 | 确定性边收集 + Kruskal canonical key | 同上 |

结论：不是代码 bug，是 MST 在等权边极多时的数学非唯一性。除非知道参考实现的确切平局策略，否则无法完全匹配——但任意有效 MST 在语义上都是正确的。

### 3.5 测试覆盖的边界情况

除了标准测试用例覆盖的场景外，我在开发过程中额外关注了以下边界：

| 边界情况 | 覆盖方式 | 位置 |
|----------|----------|------|
| 点不存在 | `QUERY_PLACE P9999` → `ERROR place_not_found` | case_01 第 4 行 |
| 边不存在 | `DELETE_ROAD` 不存在的边 → `ERROR road_not_found` | CommandProcessor 前置检查 |
| 重复插入 | `ADD_PLACE` 已存在 ID → `ERROR place_already_exists` | case_01 第 8 行 |
| 不可达 | 两点之间无 open 路径 → `NO_PATH` | Dijkstra 终点不可达 |
| 图不连通 | MST 边数 ≠ V-1 → `DISCONNECTED` | Kruskal 收尾检查 |
| 字段校验 | 非法字段/非法值 → `ERROR invalid_field` | UPDATE_PLACE/UPDATE_ROAD |
| 时间窗口 | 查询时刻某节点关闭 → 路径绕行 | mixed_time_100 + 对抗样例 |
| 必经点顺序 | MUST_PASS 分段拼接，段间去重 | random_sparse_100 |
| 等权路径 | DIST/TIME 平局选字典序更小前驱 | grid_100 |
| 等权 MST | 多个等权边 → 确定性排序选唯一 MST | random_sparse_10000 |
| 大规模 | chain_1000（998 关键节点）、random_sparse_10000（10000 节点） | large_cases |
| 分层图贪心反例 | A-X-D 5+50 vs A-B-C-D 10+10+10，K=1 | case_layered_01 |

---

## 4. 遇到的问题与解决思路

### 4.1 MST 等权边不确定性

**问题**：grid_100、ring_100、random_sparse_100、random_sparse_10000 四个用例的 MST 输出与标准答案不一致，但总权相同。

**诊断**：逐步排查发现 Kruskal 在处理等权边时，`unordered_map` 的迭代顺序不确定，导致边的初始收集顺序和排序后的等权边顺序均不确定。修复步骤：
1. 边收集改为按排序后的 `AllPlaceIds` 确定性遍历，替代 `AllEdges` 的 `unordered_map` 随机迭代
2. 排序增加 canonical key 副键（`min(from,to)|max(from,to)` 字典序）
3. 修复后 medium 三个用例全部通过，仅 random_sparse_10000 残余（MST 不唯一问题，见 §3.4）

### 4.2 Dijkstra 等权路径选择

**问题**：grid_100 的 SHORTEST TIME 输出路径与标准答案不同（总时间相同，均为 18）。

**诊断**：grid 图中存在多条等长时间的最短路径。原实现中 `nd < dist[v]` 严格不等，保留先找到的路径。但先找到哪条取决于邻接边遍历顺序（`unordered_map` 迭代），不确定。

**修复**：改为 `nd < dist[v] || (nd == dist[v] && u < prev[v])`——等权时选字典序更小的前驱。这虽不能保证全局路径字典序最小，但在 grid 等常见图结构中与参考输出一致。

### 4.3 FAST 边输出排序

**问题**：分层图 large 测试 random_sparse_1000 中，FAST 边输出顺序与标准答案不一致。

**诊断**：路径回溯给出的 FAST 边顺序是路径遍历顺序。但评测接口要求按 `(min, max)` 字典序输出。

**修复**：在 `cmdShortestK` 输出前对 `fast_edges` 做 `std::sort`，按 canonical 字典序排列。

### 4.4 图形化界面 SHORTEST_K 缺失

**问题**：图形化界面最初只支持 DIST/TIME 两种模式，缺少分层图 SHORTEST_K 的交互支持。

**修复**：在 HTML 中前端实现完整分层图 Dijkstra（`dijkstraK` 函数），双向状态转移，路径回溯与用券边标记。画布上用券边以 ⚡ 图标和加速后时间标注。新增 K 值输入框（0~10），模式下拉联动显隐。

---

## 附录：项目文件结构

```
CampusNavigation/
├── main.cpp                  # CLI 入口
├── LGraph.h/cpp              # 图 ADT（嵌套 unordered_map）
├── LocationInfo.h            # 地点信息结构体
├── GraphException.h          # 异常类
├── CsvIO.h/cpp               # CSV 读写
├── Algorithm.h/cpp           # 图算法（DSU/Dijkstra/MST/Critical/SHORTEST_K）
├── CommandProcessor.h/cpp    # 命令解析与分发
├── graph_ui.html             # 图形化界面（拓展3）
├── 图形化界面技术说明.md
└── CMakeLists.txt

自定义数据集/                 # 拓展2
├── 华师大普陀校区/
│   ├── places.csv / roads.csv
│   └── 说明文档.md
└── 对抗样例/
    ├── places.csv / roads.csv
    └── 说明文档.md

数据结构设计说明.md           # LGraph 设计文档
```

---

## 5. AI 协作记录

本项目在 Claude Code（DeepSeek v4 pro）协助下完成。以下记录关键交互轨迹。

### A. 关键提示词对话

#### 对话 1：分层图 SHORTEST_K 算法设计

**场景**：拓展 1 要求实现 K 张加速券的最短路径。我一开始想的是先跑 Dijkstra 求出最短路，然后在路径上挑最长的那条边用券——用贪心在直觉上很合理。

**我的提示词**：
> SHORTEST_K 是不是可以先跑 TIME 模式 Dijkstra，然后在结果路径上挑 walk_time 最大的几条边用券？但感觉有点不对劲，你帮我想想有没有反例。

**AI 回应要点**：AI 直接给了一个反例：A-B-C-D 全 10min vs A-X-D（5min + 50min），K=1。贪心先走 A-B-C-D，在 AB 上用券得 4+10+10=24；但如果走 A-X-D，在 XD 上用券得 5+17=22。结论：路径选择和用券决策必须同时优化，不能分两阶段做。然后给出了分层图 DP 的方案——把"用了 k 张券"作为状态维度展开成 K+1 层图，每层跑 Dijkstra，层间通过"用券"边连接。

**我的处理**：看了反例之后恍然大悟，完全接受分层图方案。我追问了路径回溯怎么实现——怎么知道哪条边用了券？AI 给了 prev_node[v][k] + prev_k_map[v][k] + coupon[v][k] 三个数组分别记录前驱地点、前驱 k 值、是否用券。

**理由**：这个反例是我自己没想到的。如果当时按贪心做了，K=1 的测试用例必挂。分层图思路也比我最初的方案优雅得多。

---

#### 对话 2：MST 等权边排序——反复试了五种方案

**场景**：第一次跑完全部 13 个必做用例，grid_100、ring_100、random_sparse_100、random_sparse_10000 四个 MST 全挂了。但诡异的是总距离都一样，就边选的不同。我当时以为是 Kruskal 写错了，查了半天逻辑没问题。

**我的提示词**：
> 四个用例 MST 全错但总距离都对，什么情况？diff 看着就是某些等权边选的不一样。我的 Kruskal 排序只按 distance，是不是等权边顺序的问题？

**AI 回应要点**：AI 确认是等权边处理顺序导致的，建议排序加一个副键——canonical key（min(from,to)|max(from,to)）字典序。我照做了，medium 三个过了，但 random_sparse_10000 还是不行。我又试了 raw from-to 排序、string concat 排序，都不行。AI 说要不换 Prim 试试？我换了，但是没有什么用。最后 AI 自己承认：10000 节点等权边太多了，MST 在数学上根本不唯一，你选的和参考实现选的都是对的，只不过恰好不一样。46/9999 的差异不是 bug。

**我的处理**：折腾了五轮——canonical key → raw from-to → string concat → Prim → 回退 Kruskal。最后保留 Kruskal + 确定性边收集 + canonical key。Prim 的尝试代码后来也删了，因为换了也没用，还多一套实现要维护。

**理由**：这个过程教会我一件事——测试通过率不是 100% 不一定是你写错了，有时候是问题本身有多解。关键是要能说清楚为什么不一样、为什么两个都对。

---

#### 对话 3：Dijkstra 等权路径——"凭什么他走 P0002 我走 P0011"

**场景**：grid_100 的 SHORTEST TIME 输出是 PATH TIME 18，总时间对了，但路径节点序列跟答案不一样。我的路径走 P0001→P0011→P0021→...，答案走 P0001→P0002→P0003→...。都是 18，但我怎么知道谁的是"对的"？

**我的提示词**：
> grid_100 TIME 模式，我输出的路径跟答案不一样但总时间相同。这是 10x10 的网格，绕来绕去都是 18。我的 Dijkstra 发现新路径时用 `<` 不更新前驱，但先找到哪条取决于 unordered_map 的遍历顺序。怎么让它跟答案一致？

**AI 回应要点**：AI 建议在距离相等时加一个 tie-breaker——比较前驱节点 ID 的字典序，`nd == dist[v] && u < prev[v]` 时才更新。这样等权路径会选字典序更小的前驱，大概率跟参考实现一致。

**我的处理**：采纳。把 `nd < dist[v]` 改成 `nd < dist[v] || (nd == dist[v] && u < prev[v])`，两处 Dijkstra 都改了。grid_100 通过了。

**理由**：这不算"标准答案"式的正确性——只是让输出跟参考实现保持一致。如果参考实现用了不同的 tie-breaker（比如选后找到的），这个策略就不灵了。但这已经是比较合理的做法，在网格图和大多数拓扑下都能匹配。

---

#### 对话 4：华师大周边数据调研

**场景**：拓展 2 需要构造 12~20 节点、20~40 边的真实场景微数据集。

**我的提示词**：
> 上网查华东师范大学普陀校区旁边的地铁跟公交网络和大的商圈，整理一下。

**AI 回应要点**：通过 WebSearch 获取了金沙江路站（3/4/13号线）、大渡河路站（13号线）、中山公园站（2/3/4号线）、隆德路站（11/13号线）、曹杨路站（3/4/11号线）等地铁站信息；环球港、龙之梦、华联商厦、近铁城市广场等商圈；67路、754路、856路、947路等公交线路。

**我的处理**：修改后采纳。筛选了离学校最近的 12 个节点，设计 40 条边（步行 + 地铁双层网络），并加入差异化时间窗口。

**理由**：AI 提供的搜索结果是原始地理数据，需要我根据项目 schema 做结构化建模——比如哪些点之间应该有边、边的 distance/walk_time 如何设定、哪些点的时间窗口能构成 TIMED_SHORTEST 的测试场景。

---

#### 对话 5：图形化界面技术选型

**场景**：拓展 3 需要图形化界面，要决定技术方案。

**我的提示词**：
> HTML + Canvas 方案（零依赖，浏览器打开即可）。

**AI 回应要点**：确认为最佳方案，给出了 Canvas 渲染架构、力导向布局算法、鼠标交互设计等具体实现建议。

**我的处理**：接受技术路线，但多次提出修改要求——默认数据必须用项目自带的 87 节点数据而非 case_01、必须支持 TIMED_SHORTEST 和 SHORTEST_K、必须去掉拖拽只保留点击。

**理由**：技术选型认可，但具体需求需要我根据项目实际情况把关。

---

#### 对话 6：对抗样例设计

**场景**：拓展 2 要求构造能让错误实现暴露问题的图数据。

**我的提示词**：
> 对抗目标：TIMED_SHORTEST 中间节点时间窗检查。构造"短但有约束"vs"绕路无约束"的对比路径。

**AI 回应要点**：提出了 8 节点校园微型图的构造方案——P0003（食堂，06:00-21:00）是短路径的必经节点，P0006（体育场，06:00-22:00）是绕路但开放更晚的替代。22:00 查询时短路径因食堂关闭而不可用，正确实现走绕路 12min，错误实现走短路径 4min。

**我的处理**：接受图结构设计，自行验证了 LOAD 和关键命令的正确性。

**理由**：对抗图的设计思路清晰，能有效区分"只检查起止点"和"检查全部中间节点"两种实现。

---

### B. 调试故事

#### 1：MST 四个用例同时失败

**现象**：首次跑全部 13 个必做用例，grid_100、ring_100、random_sparse_100、random_sparse_10000 四个用例的 MST 输出均与答案不一致。所有不匹配的 MST 总距离与答案相同，但边集合不同。

**诊断信息**：diff 输出显示均为 MST 行差异。确认自己的 Kruskal 实现逻辑正确——边按 distance 排序 → DSU 贪心选边 → 验证边数 = V-1。

**AI 的修复建议**：检查 `AllEdges` 收集顺序——它依赖 `unordered_map` 迭代，顺序不确定。建议改为按排序后的 `AllPlaceIds` 确定性遍历收集边；排序增加 canonical key (`min(from,to)|max(from,to)`) 副键平局。

**最终方案**：采纳了确定性边收集 + canonical key 排序。修复后 grid_100、ring_100、random_sparse_100 三个用例通过。random_sparse_10000 残余——这是 10000 节点等权边极多导致的 MST 数学非唯一性（9953/9999 条边一致，双方总权相同）。

---

#### 2：FAST 边输出顺序错误

**现象**：分层图 large 测试 random_sparse_1000 中，`FAST` 后面的用券边列表顺序与答案不一致，但边集合相同。diff 显示 `P0216-P0419` 和 `P0216-P0559` 的位置互换。

**诊断信息**：路径回溯构建 `fast_edges` 的顺序是路径遍历顺序（从起点到终点）。两边的 canonical form 都以 P0216 开头，按字典序 P0216-P0419 应排在 P0216-P0559 前面。

**AI 的修复建议**：在输出前对 `fast_edges` 做 `std::sort`，按 `(first, second)` 字典序排列。

**最终方案**：采纳。在 `cmdShortestK` 中添加一行排序即可修复。这是一个典型的"算法正确但输出格式不符合规范"的 bug。

---

#### 3：UI 模式缺失

**现象**：图形化界面最初只实现了 DIST 和 TIME 两种查询模式。查看拓展要求后发现缺少 TIMED_SHORTEST 和 SHORTEST_K 的交互支持。

**诊断信息**：模式选择器只有两个 `<option>`，`findPath` 函数只调用 `dijkstra`。需要在 JS 中实现对应的算法函数，并在 UI 中添加参数输入控件。

**AI 的修复建议**：分两次补全——先加 SHORTEST_K（分层图 Dijkstra + K 值输入框），后加 TIMED_SHORTEST（时刻约束 Dijkstra + HH:MM 时间输入 + DIST/TIME 子模式选择）。

**最终方案**：采纳全部。`dijkstraK`（~50 行）和 `dijkstraTimed`（~25 行）均在前端实现。结果面板和画布边标注同步更新。

---

### C. 我拒绝过 AI 的建议

#### 拒绝 Prim 替换 Kruskal

**AI 的建议**：在 MST 四个用例同时失败的调试过程中，AI 建议将 Kruskal 算法替换为 Prim 算法——"从最小 place_id 出发的 Prim 可能匹配参考输出的 MST 边集合"。

**我为什么不采纳**：

1. **换算法不能根本解决问题**。Kruskal 和 Prim 在等权边极多的图上（如 10000 节点随机稀疏图）会产生等价但不同的 MST。这是 MST 数学上的非唯一性，与算法选择无关。换成 Prim 只是从"Kruskal 选的 MST"变成"Prim 选的 MST"，仍然不一定匹配参考输出。
2. **Kruskal + DSU 已在项目中实现并经过验证**。DSU 在阶段 4.1 实现、MST 的 Kruskal 使用 DSU 做连通性判断、DSU 的路径压缩和按秩合并均正确。换成 Prim 需要重写 MST 函数，增加维护成本而无实际收益。
3. **实际结果证实了我的判断**。为了完整验证，我还是让 AI 实现了 Prim 版本并测试——random_sparse_10000 仍然不匹配（总权相同，边集合不同），证实了"换算法不解决问题"。

**我的替代方案**：保留 Kruskal，优化确定性——改为从排序后的 `AllPlaceIds` 遍历收集边（替代 `AllEdges` 的 `unordered_map` 随机迭代），排序增加 canonical key 副键平局。修复后 medium 三个用例通过，large 一个残余在报告中以数学非唯一性如实说明。

---

### D. 总结

**AI 在本项目中扮演的角色**：AI 主要作为辅助工具，帮助完成拓展任务（分层图、Tarjan、图形化界面）和调试疑难 bug，以及起草文档。核心必做算法的代码（连通分量、Dijkstra、必经点、MST、关键节点/边）由我编写。AI 的价值体现在：加速了拓展功能的开发、规范了代码格式，提供了 MST 等权边问题的诊断视角、以及承担了 HTML/CSS/JS 这些非核心但繁琐的 UI 代码。

**具体分工**：

| 模块 | 谁写的 | 说明 |
|------|:--:|------|
| LGraph / CsvIO / main.cpp | 我 | 骨架填充，数据结构和 IO 是基础 |
| DSU 并查集 | 我 | 路径压缩 + 按秩合并，几行代码 |
| 连通分量 BFS | **我** | 含 blocked_vertex/edge 参数，供复用 |
| Dijkstra 最短路径 | **我** | DIST/TIME 双模式，优先队列 |
| 时刻约束最短路径 | AI 辅助 | 在 Dijkstra 基础上加时间检查 |
| 必经点路径 | **我** | 分段调 Dijkstra，拼接去重 |
| MST Kruskal | **我** | AI 帮忙修了等权边排序的确定性 bug |
| 关键节点/边（暴力） | **我** | 复用 bfsComponent，屏蔽参数 |
| Tarjan 加分项 | AI 辅助 | 非递归 DFS，我验证了与暴力一致 |
| SHORTEST_K 分层图 | AI 辅助 | K 层状态图 Dijkstra |
| CommandProcessor 命令 | AI 辅助 | 接入算法函数 + 格式化输出 |
| graph_ui.html | AI 辅助 | 1200 行 Canvas 界面，我提需求 AI 写 |
| 自定义数据集 | AI 搜索 + 我建模 | 华师大周边地理数据，我设计边和权值 |
| 对抗样例 | AI 提出思路 + 我验证 | TIMED_SHORTEST 时间检查 |
| 代码格式与规范 | AI 辅助 | 命名、缩进、注释、头文件 include 顺序 |
| 实验报告 / 拓展报告 | AI 初稿 + 我修改 | 算法描述和设计决策是我写的 |

**我最有把握的部分**：数据结构设计——嵌套 unordered_map 的选型理由充分（O(1) 增删查、删顶点仅 O(deg(v))、无需索引映射），复杂度分析完整，与项目所有功能兼容。

**我最不放心的部分**：random_sparse_10000 的 MST 差异（见 §3.4），以及分层图状态转移的在状态数扩展比较大时会不会超时——虽然测试都过了，但如果 K 很大（接近 10）且节点上千，O(K·V log V) 的常数项会比普通 Dijkstra 大不少。
