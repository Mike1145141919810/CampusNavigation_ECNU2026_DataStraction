# 校园导航系统 — 对标 Winter-Iris 的借鉴实现计划

## 对标目标

在不偏离课程评测接口（CLI + 命令规范）的前提下，吸收 [Winter-Iris/CampusNavigation](https://github.com/Winter-Iris/CampusNavigation) 中值得借鉴的工程实践，提升本项目在以下三方面的完成度：

1. 核心算法实现质量（Dijkstra / MST / 连通分析）。
2. 输入输出稳定性（CSV 容错、错误处理、批量测试）。
3. 工程可维护性（模块职责、日志与调试、性能验证流程）。

---

## 现状对比（本地骨架 vs Winter-Iris）

| 维度 | 本地项目（当前） | Winter-Iris 项目 | 可借鉴结论 |
|------|------------------|------------------|------------|
| 项目形态 | CLI 评测骨架 + TODO | Qt GUI + 图算法完整实现 | 算法与工程做法可借鉴，界面层不必照搬 |
| 图结构 | 待实现（LGraph） | 邻接表 + 名称/ID映射 | 借鉴“索引 + 邻接”思想，改造成 place_id 版本 |
| 最短路 | 待实现 | 优先队列 Dijkstra | 直接借鉴算法框架 |
| MST | 待实现 | Kruskal 思路与输出 | 可直接迁移到 CLI 输出规范 |
| CSV读写 | 待实现 | 跳表头、格式校验、错误日志 | 建议完整吸收 |
| 测试习惯 | 课程测试集已提供 | 多规模测试与日志留痕 | 借鉴“分规模验证 + 结果留档” |

---

## 值得重点借鉴的地方

### 1. 算法实现方式（高优先级）

**可借鉴点：**
- Dijkstra 采用优先队列优化，避免朴素 O(V^2) 实现。
- 路径重建时维护前驱数组（或映射），并单独处理不可达与重建失败场景。
- MST 使用 Kruskal + 并查集，工程实现清晰、稳定。

**落地到本项目：**
- 在 `Algorithm::GetShortestPath()` 和 `Algorithm::GetTimedShortestPath()` 里统一一套 Dijkstra 主逻辑，仅通过 `PathMode` 切换权重来源。
- 在 `Algorithm::MinimumSpanningTree()` 统一只取 open 边，先排序再做 DSU 合并。
- 在 `CommandProcessor` 中把算法结果严格映射成命令规范要求的输出格式，避免“算法正确但格式失分”。

### 2. 输入鲁棒性与错误处理（高优先级）

**可借鉴点：**
- CSV 读取时显式跳过表头。
- 行字段数量不合法时立即报错并中止本次读取。
- 文件打开失败给出可定位原因（路径 + 系统错误）。

**落地到本项目：**
- `CsvIO::ReadPlaces()` / `CsvIO::ReadRoads()` 统一做：空行过滤、表头识别、字段数检查、类型转换检查。
- `CommandProcessor` 把 GraphException 映射为统一 `ERROR xxx` 文本，保证评测输出稳定。
- 为 `LOAD` 增加“部分失败回滚”策略：读取或插入失败时不污染旧图。

### 3. 工程组织与模块边界（中优先级）

**可借鉴点：**
- Graph 层负责数据与算法基础能力，UI/交互层只做参数采集和结果展示。
- 复杂功能（最短路、MST）保持在图算法模块中，避免界面/命令层掺杂过多逻辑。

**落地到本项目：**
- 保持 `LGraph` 只做数据结构与基础操作。
- 保持 `Algorithm` 只做纯算法。
- 保持 `CommandProcessor` 只做参数解析、调用与输出。

### 4. 大规模数据测试意识（中优先级）

**可借鉴点：**
- Winter-Iris 仓库中有对大规模图数据的读入和算法运行记录（包含日志时间点）。

**落地到本项目：**
- 使用本地 `测试数据_v2/` 按 small → medium → large 分层回归。
- 每次阶段性完成后固定跑一组 smoke case，防止后续改动破坏已通过功能。

---

## 不建议直接照搬的部分

1. Qt GUI 交互流程。
本项目评分核心是 CLI 命令接口，图形界面不是必做项，照搬 GUI 价值有限。

2. 名称驱动的节点主键策略。
Winter-Iris 主要围绕地点名称进行操作；本项目规范以 `place_id` 为主键，必须保证 ID 稳定唯一。

3. 非命令规范的日志输出。
Winter-Iris 偏用户友好日志（中文描述段落），而本项目评测依赖严格文本格式，需避免额外噪声输出到标准输出。

---

## 分阶段借鉴落地计划（按 plan 风格）

### 阶段 1：先把“可跑通”能力搭起来（LGraph + CsvIO）

**目标：** 完成点边增删改查、CSV 读写，命令层可执行基础维护命令。

| 任务 | 借鉴来源 | 验收标准 |
|------|----------|----------|
| 1.1 图存储实现 | 邻接结构思路 | `VertexCount/EdgesCount/exist_*` 正确 |
| 1.2 点边一致性维护 | 图增删同步策略 | 删除点后相关边全清理 |
| 1.3 CSV鲁棒读写 | 跳表头+格式检查 | 对异常文件给出 ERROR，不崩溃 |
| 1.4 命令异常映射 | 错误码收敛 | 与命令规范一致输出 |

### 阶段 2：借鉴算法主干（Shortest / Components / MST）

**目标：** 先完成最常见评测算法，并保证输出格式严格一致。

| 任务 | 借鉴来源 | 验收标准 |
|------|----------|----------|
| 2.1 Dijkstra 双模式 | 优先队列最短路 | `SHORTEST` small/medium 全通过 |
| 2.2 连通分量统计 | BFS/DFS 实现习惯 | `COMPONENTS` 输出个数和规模正确 |
| 2.3 Kruskal + DSU | MST工程实现 | `MST` 在连通/非连通场景均正确 |

### 阶段 3：实现本项目特有约束（Timed / MustPass / Critical）

**目标：** 在基础算法上加课程特有约束，不做功能偷换。

| 任务 | 说明 | 验收标准 |
|------|------|----------|
| 3.1 TIMED_SHORTEST | 点时间窗 + open 边过滤 | 路径节点均满足开放时段 |
| 3.2 MUST_PASS | 分段最短路拼接 | 任一段不可达则 NO_PATH |
| 3.3 CRITICAL | 按课程要求先做重算版 | 结果与参考答案一致 |

### 阶段 4：测试与报告打磨

**目标：** 对齐评分点，形成可答辩材料。

| 任务 | 说明 |
|------|------|
| 4.1 分层测试脚本 | small/medium/large + 拓展集 |
| 4.2 复杂度统计 | 各核心操作与算法写入报告 |
| 4.3 借鉴说明 | 说明“借鉴了什么、为何适配、为何没照搬GUI” |

---

## 可直接执行的借鉴清单（短清单）

1. 先照 Dijkstra + 前驱回溯完成 `SHORTEST`，这是收益最高的一步。
2. 在 CsvIO 一次性补齐表头识别、字段校验、空行跳过和异常返回。
3. 命令层统一异常到规范错误码，避免输出杂音影响判题。
4. 完成 Kruskal + DSU 后立刻跑 medium + large 的 MST 用例做性能确认。
5. 把 timed/must_pass/critical 视为“约束叠加”，复用已有最短路和连通性能力。

---

## 风险与规避

1. 风险：借鉴 GUI 项目导致命令输出风格偏离规范。
规避：所有用户友好日志只允许走 `stderr` 或调试开关，不进入标准输出。

2. 风险：最短路实现可用，但路径重建细节导致答案格式错误。
规避：对每个 `PATH` 命令做“总代价 + 节点序列”双重断言。

3. 风险：无向图边重复计数，影响 `EdgesCount` 与 `MST`。
规避：统一边规范化键 `(min(u,v), max(u,v))` 或插入时双向存储但计数只加一次。

---

## 结论

Winter-Iris 仓库最值得借鉴的，不是界面，而是“算法实现的工程化细节 + CSV鲁棒处理 + 大规模测试习惯”。

如果按本计划落地，你的项目能在保持课程命令规范完全兼容的前提下，显著提升正确率、稳定性和报告说服力。