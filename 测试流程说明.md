# 测试流程说明

## 1. 编译测试程序

```bash
# 在项目根目录下
cmake -S CampusNavigation -B build -G "Ninja"
cmake --build build --config Debug
```

## 2. 运行测试

```bash
cd CampusNavigation
../build/test_lgraph.exe
```

测试程序需要将工作目录切换到 `CampusNavigation/`（因为测试会读取同目录下的 `places.csv` 和 `roads.csv`）。

## 3. 测试覆盖

一共 11 个测试用例，全部通过后输出 `11 passed, 0 failed`：

### 3.1 基础 CRUD

| 测试 | 验证内容 |
|------|---------|
| Vertex CRUD | InsertVertex / GetVertex / UpdateVertex（5字段）/ DeleteVertex |
| Edge CRUD | InsertEdge / GetEdge / UpdateEdge（3字段）/ DeleteEdge，无向图反向边同步 |
| Road status | CloseRoad / OpenRoad 及反向边状态同步 |

### 3.2 遍历与查询

| 测试 | 验证内容 |
|------|---------|
| AllPlaceIds | 返回所有顶点 ID |
| AllEdges | 无向图去重（字典序过滤），only_open 参数过滤 |
| GetAdjacentEdges | 查询指定顶点的邻接边 |
| GetPlacesByCategory | 按 category 筛选顶点 |

### 3.3 异常处理

| 测试 | 验证内容 |
|------|---------|
| Exceptions | 重复插入顶点/边、操作不存在顶点/边、无效字段名、无效 status 值 |

### 3.4 有向图模式

| 测试 | 验证内容 |
|------|---------|
| Directed graph mode | 有向边不生成反向边、删除顶点时清理入边、边计数正确 |

### 3.5 真实数据加载

| 测试 | 验证内容 |
|------|---------|
| Real data | 加载 `places.csv`（87地点）和 `roads.csv`（26道路），验证顶点/边数据正确、双向存储、AllEdges 去重、分类筛选、邻接查询、删除顶点后边清理和计数更新 |

### 3.6 边计数一致性

| 测试 | 验证内容 |
|------|---------|
| Edge count consistency | 删除顶点后 `edge_count_` 正确递减 |

## 4. 添加新测试

在 `test_lgraph.cpp` 中新增 `void test_xxx()` 函数，然后在 `main()` 中调用即可。使用以下宏：

```cpp
TEST("测试名称");       // 输出 [TEST] 标记
CHECK(条件, "说明");    // 断言，失败时输出 FAIL
PASS();                 // 标记测试通过
```

## 5. 文件位置

- 测试源码：`CampusNavigation/test_lgraph.cpp`
- 测试 target 定义：`CampusNavigation/CMakeLists.txt`
- 可执行文件：`build/test_lgraph.exe`
