# LGraph 数据结构设计说明

## 1. 选型：嵌套 unordered_map

```cpp
std::unordered_map<std::string, LocationInfo> vertices_;  // place_id → 顶点
std::unordered_map<std::string,
    std::unordered_map<std::string, EdgeNode>> adj_;       // from → {to → 边}
bool directed_;
int edge_count_;
```

## 2. 为什么选这种结构？

- **顶点/边增删查均为平均 O(1)**，`unordered_map` 提供常数时间查找，无需维护索引映射。
- **无向图双向存储**：插入边时同时写入 `adj_[u][v]` 和 `adj_[v][u]`，删除顶点时只需遍历该顶点的邻接表即可清理所有反向边。
- **无需 ID→索引映射**：直接以 `place_id`（字符串）为主键，代码直观，减少映射维护成本。
- **10000 节点规模足够**：在课程评测规模下，空间和性能均满足要求。

## 3. 考虑过的替代方案

| 方案 | 思路 | 权衡 |
|------|------|------|
| **邻接表 + 索引映射** | `vector<vertex>` + `vector<list<edge>>`，用 `unordered_map<string,int>` 做 ID→下标映射 | 删除顶点时需要全局重排索引或标记删除，实现复杂；但遍历时缓存友好 |
| **邻接矩阵** | `vector<vector<bool>>` | 顶点增删 O(V²)，稀疏图浪费空间 |
| **单层 unordered_map + 边集** | `unordered_map<string, LocationInfo>` 存点，`unordered_set<string>` 存边 key（"u-v"） | 删除顶点时要遍历整个边集匹配，O(E) 昂贵 |

**最终选择嵌套 unordered_map** 的原因：删除顶点时只需要 O(deg(v)) 清理邻接表，无需遍历全图或重排索引，在频繁动态更新的场景下综合最优。

## 4. 操作复杂度分析

### 顶点操作

| 操作 | 时间复杂度 | 空间复杂度 | 说明 |
|------|-----------|-----------|------|
| `VertexCount()` | O(1) | O(1) | 取 `vertices_.size()` |
| `exist_vertex(id)` | O(1) 平均 | O(1) | `unordered_map::find` |
| `InsertVertex(v)` | O(1) 平均 | O(1) | 直接插入 bucket |
| `DeleteVertex(id)` | O(deg(v)) 平均 | O(1) | 无向图需遍历邻接表清理反向边 |
| `UpdateVertex(id, f, v)` | O(1) 平均 | O(1) | 查找后修改字段 |
| `GetVertex(id)` | O(1) 平均 | O(1) | 查找后返回副本 |
| `Clear()` | O(V) | O(1) | 清空 `vertices_`、`adj_`，重置 `edge_count_` |

### 边操作

| 操作 | 时间复杂度 | 空间复杂度 | 说明 |
|------|-----------|-----------|------|
| `EdgesCount()` | O(1) | O(1) | 取 `edge_count_` |
| `exist_edge(u, v)` | O(1) 平均 | O(1) | 双层 `find` |
| `InsertEdge(u, v, ...)` | O(1) 平均 | O(1) | 无向图多一次反向插入 |
| `DeleteEdge(u, v)` | O(1) 平均 | O(1) | 无向图多一次反向删除 |
| `UpdateEdge(u, v, f, v)` | O(1) 平均 | O(1) | 无向图同步反向字段 |
| `GetEdge(u, v)` | O(1) 平均 | O(1) | 双层 `find` |
| `CloseRoad / OpenRoad` | O(1) 平均 | O(1) | 委托 `UpdateEdge` |

### 遍历/查询

| 操作 | 时间复杂度 | 空间复杂度 | 说明 |
|------|-----------|-----------|------|
| `AllPlaceIds()` | O(V) | O(V) | 遍历 `vertices_` 收集 key |
| `AllEdges(only_open)` | O(V+E) | O(E) | 双层遍历 `adj_`，无向图跳过反向半 |
| `GetAdjacentEdges(id)` | O(deg(v)) | O(deg(v)) | 遍历邻接表收集 |
| `GetPlacesByCategory(cat)` | O(V) | O(V) | 全表扫描按字段筛选 |

> **注：** V = 顶点数，E = 边数（单向计数），deg(v) = 顶点的度。
> `unordered_map` 的"平均 O(1)"依赖良好的哈希函数，`std::string` 的哈希实现足够均衡，无需额外定制。
